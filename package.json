{
	"name": "vscode-elrond-c",
	"displayName": "Elrond Smart Contract Development in C",
	"description": "VS Code extension to enable Elrond smart contract development in C",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.38.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.showDebugger",
		"onCommand:extension.buildCurrentFile",
		"onCommand:extension.startDebugServer"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.showDebugger",
				"title": "Smart Contracts - show debugger"
			},
			{
				"command": "extension.buildCurrentFile",
				"title": "Smart Contracts - build current file"
			},
			{
				"command": "extension.startDebugServer",
				"title": "Smart Contracts - start (restart) debug server"
			}
		],
		"menus": {
			"editor/context": [
				{
					"command": "extension.buildCurrentFile",
					"group": "dummyGroup",
					"when": "resourceExtname == .c"
				}
			]
		},
		"configuration": {
			"title": "vscode-elrond-c",
			"properties": {
				"elrond.ideFolder": {
					"type": "string",
					"default": "/home/someone/Elrond/ide",
					"description": "Folder for ide tools."
				},
				"elrond.restApi.port": {
					"type": "number",
					"default": "8080",
					"description": "Debug server port."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^10.12.21",
		"@types/vscode": "^1.38.0",
		"glob": "^7.1.4",
		"mocha": "^6.1.4",
		"typescript": "^3.3.1",
		"tslint": "^5.12.1",
		"vscode-test": "^1.2.0"
	},
	"dependencies": {
		"@types/request": "^2.48.3",
		"@types/underscore": "^1.9.3",
		"request": "^2.88.0",
		"underscore": "^1.9.1"
	}
}
